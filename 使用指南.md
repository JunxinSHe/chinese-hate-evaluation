
# 中文仇恨识别评测工具 - 使用指南

## 一、项目结构

```
chinese-hate-evaluation/
├── config/                  # 配置文件
├── datasets/                # 数据文件
│   ├── demo.txt            # 示例数据
│   ├── test1.json          # 测试集1
│   ├── test2.json          # 测试集2
│   └── train.json          # 训练集
├── models/                  # 基底模型
│   └── Qwen2___5-7B-Instruct-GPTQ-Int4/
├── results/                 # 微调模型和推理结果
│   ├── finetuned/          # 微调好的模型
│   ├── test1_result.json   # test1详细结果
│   └── test1_result_demo.txt # test1 Demo格式
├── scripts/
│   ├── finetune_final.py    # 微调脚本（核心）
│   └── infer.py            # 推理脚本（核心）
├── requirements.txt         # 依赖库
├── README.md
├── 使用指南.md
├── 环境搭建说明.md
└── 部署文档.md
```

---

## 二、环境准备

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 检查GPU

```bash
python -c "import torch; print('CUDA available:', torch.cuda.is_available())"
```

---

## 三、模型微调

### 1. 微调前准备

- 确保训练数据 `datasets/train.json` 已准备好
- 确保基底模型 `models/Qwen2___5-7B-Instruct-GPTQ-Int4/` 已下载
- 确保显存满足要求：Qwen-7B 4bit量化微调需要8G以上显存

### 2. 执行微调

```bash
python scripts/finetune_final.py
```

**可选参数**：
```bash
python scripts/finetune_final.py \
    --model_path ./models/Qwen2___5-7B-Instruct-GPTQ-Int4 \
    --data_path ./datasets/train.json \
    --output_dir ./results/finetuned \
    --epochs 3 \
    --batch_size 1 \
    --lr 2e-5 \
    --max_len 256
```

### 3. 显存优化建议

如果显存不足，可以：
- 减小 `max_len`（如从256降到128）
- 确保 `batch_size=1`
- 启用梯度检查点（已在脚本中启用）

---

## 四、推理使用

### 1. 单条文本推理

```bash
python scripts/infer.py --text "今天天气真好"
```

### 2. 批量推理

```bash
python scripts/infer.py --input_file ./datasets/test1.json --output_file ./results/test1_result.json
```

**参数说明**：
| 参数 | 说明 | 示例 |
|------|------|------|
| `--model_path` | 微调模型路径 | `./results/finetuned` |
| `--base_model_path` | 基底模型路径 | `./models/Qwen2___5-7B-Instruct-GPTQ-Int4` |
| `--input_file` | 输入文件路径（JSON格式） | `./datasets/test1.json` |
| `--output_file` | 输出文件路径 | `./results/test1_result.json` |
| `--text` | 单条文本输入 | `"今天天气真好"` |

### 3. 输出格式

推理结果会自动保存两个文件：

**详细结果（JSON格式）**：`test1_result.json`
```json
[
  {
    "id": 1,
    "text": "文本内容",
    "prediction": "Target | Argument | Group | Hateful [END]",
    "is_hate": true/false,
    "pred_label": 1/0
  }
]
```

**Demo格式（纯文本）**：`test1_result_demo.txt`
```
Target | Argument | Group | Hateful [END]
Target | Argument | Group | Hateful [END]
```

### 4. 输出格式说明

- **Target**：仇恨对象
- **Argument**：仇恨言论内容
- **Group**：仇恨类型（Racism/Sexism/Region/LGBTQ/others/non-hate）
- **Hateful**：是否仇恨（hate/non-hate）
- **[END]**：结束标记

---

## 五、数据格式要求

### 1. 训练数据格式（JSON）

```json
[
  {
    "id": 1,
    "text": "文本内容",
    "label": 1,
    "hate_type": "种族歧视",
    "target": "某个群体",
    "argument": "仇恨言论"
  }
]
```

### 2. 测试数据格式（JSON）

```json
[
  {
    "id": 1,
    "text": "文本内容"
  }
]
```

---

## 六、常见问题

### 1. 微调相关问题

**问题**：显存不足（CUDA out of memory）
- 解决方案：
  1. 减小 `max_len` 参数
  2. 保持 `batch_size=1`
  3. 关闭梯度累积或减小累积步数

**问题**：模型加载失败
- 解决方案：
  1. 检查基底模型路径是否正确
  2. 确保模型文件完整

### 2. 推理相关问题

**问题**：输出格式不正确
- 解决方案：
  1. 检查 `infer.py` 中的系统提示词
  2. 确保输出包含 `|` 和 `[END]`

**问题**：推理速度慢
- 解决方案：
  1. 确保使用GPU加速
  2. 减小 `max_new_tokens` 参数

---

## 七、快速开始

### 完整流程示例

1. **微调**（已完成）：
```bash
python scripts/finetune_final.py
```

2. **推理**：
```bash
python scripts/infer.py --input_file ./datasets/test1.json --output_file ./results/test1_result.json
```

3. **查看结果**：
```bash
# 查看Demo格式结果
type results\test1_result_demo.txt
```

---

## 八、注意事项

1. 确保使用正确的模型路径
2. 推理输出格式必须严格符合要求
3. 建议先在小批量数据上测试
4. 注意显存使用情况

---

## 九、联系方式

如有问题，请查看项目文档或联系技术支持。

