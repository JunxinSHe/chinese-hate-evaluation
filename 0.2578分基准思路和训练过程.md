
# 0.2578分 - 仇恨言论识别基准思路和训练过程

## 项目概述

本项目使用 Qwen2.5-7B-Instruct-GPTQ-Int4 进行 LoRA 微调，实现仇恨言论四元组识别，在测试集上获得 **0.2578分**。

---

## 最终成绩

| 指标 | 数值 |
|------|------|
| test1_result 分数 | 0.2578 |
| 训练时间 | 约 40-50 分钟（RTX 4070） |
| 显存占用 | 约 10-12 GB |

---

## 核心思路

### 1. 问题分析

**为什么之前分数低？**
- ❌ 训练时自己构造输出，没有使用训练数据中已有的 `output` 字段
- ❌ 推理时后处理过度，反而弄乱了输出
- ❌ training data 的 `output` 字段本身就是正确的四元组格式

**成功关键**：
- ✅ **完全使用训练数据的 output 字段**，不自己重新构造
- ✅ **训练和推理使用相同的提示词**
- ✅ **简化推理脚本**，让模型直接输出

---

### 2. 数据格式

#### 训练数据格式

```json
{
    "id": 396,
    "content": "没爹的黑孩到处扔",
    "output": "没爹的黑孩 | 到处扔 | Racism | hate [END]"
}
```

#### 推理输出格式

```
Target | Argument | Group | Hateful [END]
```

**Group 可选值**：
- `Racism` - 种族歧视
- `Sexism` - 性别歧视
- `Region` - 地域歧视
- `LGBTQ` - 性少数歧视
- `others` - 其他仇恨
- `non-hate` - 非仇恨

**Hateful 可选值**：
- `hate` - 仇恨言论
- `non-hate` - 非仇恨言论

---

### 3. 模型配置

| 配置项 | 数值 |
|--------|------|
| 模型 | Qwen2.5-7B-Instruct-GPTQ-Int4 |
| 量化 | 4-bit GPTQ |
| LoRA r | 16 |
| LoRA alpha | 64 |
| LoRA dropout | 0.1 |
| Target modules | q_proj, k_proj, v_proj, o_proj |
| Epochs | 5 |
| Learning rate | 1e-5 |
| Batch size | 1 |
| Max sequence length | 384 |
| Optimizer | AdamW |

---

## 训练过程

### 阶段 1：环境准备

#### 依赖安装

```bash
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install transformers peft datasets auto-gptq tqdm
```

#### GPU 环境检查

```python
import torch
print("GPU 可用:", torch.cuda.is_available())
```

---

### 阶段 2：模型微调

#### 微调脚本位置

`scripts/finetune_final.py`

#### 关键代码

```python
# 加载训练数据
with open(args.data_path, 'r', encoding='utf-8') as f:
    raw_data = json.load(f)

# 关键：直接使用 output 字段，不重新构造
texts = []
for example in raw_data:
    text = example.get('content', example.get('text', ''))
    output = example.get('output', '')  # 直接使用
    
    prompt = """你是一个内容安全审核专家。分析文本后严格按以下格式输出：
..."""
    full_text = prompt + output
    texts.append(full_text)
```

#### 运行微调

```bash
cd h:\chinese-hate-evaluation\chinese-hate-evaluation
python scripts/finetune_final.py
```

#### 训练输出

```
============================================================
仇恨言论模型微调（显存优化版）
============================================================

正在加载分词器...
✅ 分词器加载成功

正在加载 GPTQ 模型...
✅ 模型加载成功

准备模型用于训练...
trainable params: 10,092,544 || all params: 1,100,291,584 || trainable%: 0.9172608558278312

正在加载训练数据...
✅ 数据预处理完成，共 4000 条
示例数据: 你是一个内容安全审核专家...

训练配置:
  Epochs: 5
  Batch Size: 1
  Learning Rate: 1e-05
  Steps per Epoch: 4000
  Total Steps: 20000

============================================================
开始训练...
============================================================
Epoch 1/5: 100%|████████████████████| 4000/4000 [08:23<00:00, loss=0.8123, avg_loss=0.9145]
...
```

---

### 阶段 3：推理

#### 推理脚本位置

`scripts/infer.py`

#### 运行推理

```bash
python scripts/infer.py --input_file ./datasets/test1.json --output_file ./results/test1_result.json
```

#### 推理输出格式

**test1_result_demo.txt** (项目要求格式)：
```
飞周兄弟 | 还不认亲？ | Racism | hate [SEP] 飞周兄弟 | 你还袭击？ | Racism | hate [END]
广黑吧 | 没了吗？ | Region | hate [END]
卫辉官媒 | 错误 | non-hate | non-hate [END]
...
```

---

## 关键改进点总结

| 改进项 | 说明 | 效果 |
|--------|------|------|
| 使用训练数据的 output | 直接用正确格式，不自己构造 | 最关键改进 |
| 训练推理提示词一致 | 相同的系统提示，相同的格式 | 大幅提升 |
| LoRA r 从 8 到 16 | 增加模型容量 | 小幅提升 |
| Epochs 从 3 到 5 | 充分训练 | 小幅提升 |
| 简化推理后处理 | 让模型直接输出，不过度处理 | 提升稳定性 |
| 去掉 datasets 库 | 改用 PyTorch 原生 Dataset | 提升稳定性 |
| 优化器改 adamw_torch | Windows 兼容 | 无问题 |

---

## 文件结构

```
chinese-hate-evaluation/
├── config/
├── datasets/
│   ├── train.json         # 训练数据
│   ├── test1.json         # 测试集 1
│   └── test2.json         # 测试集 2
├── models/
│   └── Qwen2___5-7B-Instruct-GPTQ-Int4/
├── results/
│   ├── finetuned/         # LoRA 微调模型
│   ├── test1_result.json
│   └── test1_result_demo.txt
├── scripts/
│   ├── finetune_final.py  # 微调脚本（核心）
│   └── infer.py           # 推理脚本（核心）
└── requirements.txt
```

---

## 后续优化方向

1. **调整学习率** - 尝试 5e-6 或 2e-5
2. **增加 max_len** - 尝试 512
3. **调整 LoRA** - 尝试 r=32
4. **更多数据** - 如果有更多标注数据可以添加
5. **Ensemble** - 多个模型集成

---

## 总结

这次成功的关键是**"对齐"**：
- ✅ 训练和推理的提示词对齐
- ✅ 训练和推理的输出格式对齐
- ✅ 直接使用训练数据的正确标注

**0.2578分** 是一个很好的基准成绩！

---

## 作者

h:\chinese-hate-evaluation\chinese-hate-evaluation

## 日期

2026-05-30

