# 中文仇恨识别评测工具 - 部署文档

## 一、项目部署
### 1. 项目结构说明
```
chinese-hate-evaluation/
├── config/                # 配置文件目录
│   ├── dataset_config.yaml    # 数据集配置
│   ├── model_config.yaml      # 模型配置
│   └── training_config.yaml   # 训练参数配置
├── datasets/              # 数据集存放目录
├── examples/              # 使用示例代码
├── models/                # 训练好的模型存放目录
├── results/               # 处理结果、推理结果、评测报告存放目录
├── src/                   # 源代码目录
│   ├── data/                  # 数据处理模块
│   ├── inference/             # 推理评测模块
│   └── model/                 # 模型微调模块
├── requirements.txt       # 依赖声明文件
└── README.md              # 项目说明文档
```

### 2. 项目部署步骤
1. 按照《环境搭建说明》完成Python环境和依赖安装
2. 准备数据集：将JSONL格式的数据集存放到`datasets/`目录下
3. 检查配置文件：根据实际需求修改`config/`目录下的配置文件
4. 运行测试验证：执行示例代码验证项目功能是否正常
```bash
# 运行数据处理示例
python examples/data_process_example.py

# 运行推理示例（需要先部署Ollama模型）
python examples/inference_example.py
```

---

## 二、Ollama模型部署（推理场景需要）
推理模块基于Ollama运行，需要先部署Ollama服务和对应模型。

### 1. 安装Ollama
```bash
# Linux 一键安装
curl -fsSL https://ollama.ai/install.sh | sh

# macOS/Windows 下载安装包：https://ollama.ai/download
```

### 2. 启动Ollama服务
```bash
# Linux 系统安装后会自动启动服务，可通过以下命令查看状态
systemctl status ollama

# 手动启动服务（若未自动启动）
ollama serve
```

### 3. 拉取基础模型
```bash
# 拉取Qwen-7B基础模型（推荐）
ollama pull qwen:7b

# 或拉取Qwen-14B模型（效果更好，资源要求更高）
ollama pull qwen:14b
```

### 4. 导入微调后的仇恨识别模型
如果已经有微调好的模型文件，可通过以下方式导入：
```bash
# 1. 创建Modelfile，内容示例：
FROM qwen:7b
PARAMETER temperature 0.0
PARAMETER stop <|im_end|>
SYSTEM "你是一个仇恨言论识别助手，你需要判断输入的文本是否属于仇恨言论，输出0表示非仇恨言论，输出1表示仇恨言论。"

# 2. 导入模型
ollama create qwen_hate_finetuned -f Modelfile

# 3. 验证模型是否可用
ollama run qwen_hate_finetuned "你好"
# 应输出 0
```

### 5. 验证Ollama服务连通性
```bash
# 查看本地模型列表
ollama list

# 测试API调用
curl http://localhost:11434/api/generate -d '{
  "model": "qwen:7b",
  "prompt":"你好",
  "stream": false
}'
# 应正常返回响应结果
```

---

## 三、配置文件说明
### 1. 数据集配置 (config/dataset_config.yaml)
```yaml
# 数据集路径
train_path: datasets/train.jsonl
val_path: datasets/val.jsonl
test_path: datasets/test.jsonl

# 数据集划分比例（如果不指定单独的验证集、测试集路径）
split_ratio:
  train: 0.8
  val: 0.1
  test: 0.1

# 标签映射
label_map:
  0: "非仇恨言论"
  1: "仇恨言论"

# 仇恨类型映射
hate_type_map:
  "种族歧视": "racial"
  "性别歧视": "gender"
  "地域歧视": "regional"
  "宗教歧视": "religious"
  "其他": "other"
```

### 2. 模型配置 (config/model_config.yaml)
```yaml
# 基础模型名称
base_model: "qwen:7b"

# 模型加载参数
model_args:
  load_in_4bit: true
  trust_remote_code: true

# 推理参数
inference_args:
  temperature: 0.0
  max_new_tokens: 1
  top_p: 0.9
  stop: ["<|im_end|>"]
```

### 3. 训练配置 (config/training_config.yaml)
```yaml
# 训练参数
training_args:
  output_dir: models/qwen_hate_finetuned
  per_device_train_batch_size: 4
  per_device_eval_batch_size: 4
  gradient_accumulation_steps: 4
  learning_rate: 2e-5
  num_train_epochs: 3
  logging_steps: 10
  save_steps: 100
  evaluation_strategy: "epoch"
  fp16: true
  gradient_checkpointing: true

# PEFT参数
peft_args:
  lora_rank: 8
  lora_alpha: 32
  lora_dropout: 0.05
  lora_target_modules: ["q_proj", "v_proj"]
```

---

## 四、生产环境部署建议
### 1. 服务化部署
如果需要将推理功能作为服务提供，可使用FastAPI封装推理接口：
```python
from fastapi import FastAPI
from src.inference import OllamaInferencer

app = FastAPI(title="中文仇恨识别API")
inferencer = OllamaInferencer(model="qwen_hate_finetuned")

@app.post("/predict")
def predict(text: str):
    result = inferencer.predict_single(text)
    return {"text": text, "label": result, "label_desc": "仇恨言论" if result == 1 else "非仇恨言论"}
```

启动服务：
```bash
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 2. 性能优化
- 推理场景：使用vLLM替代Ollama提升推理吞吐量
- 训练场景：使用分布式训练（accelerate launch）提升训练速度
- 数据处理：使用多进程处理提升大数据集处理速度

### 3. 监控与日志
- 接入Prometheus监控服务运行状态
- 配置日志轮转，避免日志文件过大
- 记录推理请求、准确率等核心指标，定期生成运行报告
