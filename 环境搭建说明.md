# 中文仇恨识别评测工具 - 环境搭建说明

## 一、系统要求
### 硬件要求
- **最低配置**：CPU 4核，内存8G，磁盘20G空闲空间
- **推荐配置（训练场景）**：GPU 显存16G以上（支持CUDA），CPU 8核，内存32G，磁盘100G空闲空间
- **推荐配置（推理场景）**：CPU 8核，内存16G，磁盘50G空闲空间

### 软件要求
- 操作系统：Linux (Ubuntu 20.04+/CentOS 7+) / macOS 12+ / Windows 10+
- Python版本：3.10 ~ 3.11（推荐3.10.12）
- 包管理工具：pip 23.0+
- 可选（GPU训练）：CUDA 11.7+，cuDNN 8.5+

---

## 二、环境搭建步骤
### 1. 克隆/获取项目代码
```bash
# 假设项目代码存放路径为 /opt/chinese-hate-evaluation
cd /opt
git clone <项目仓库地址> chinese-hate-evaluation
cd chinese-hate-evaluation
```

### 2. 创建Python虚拟环境（推荐）
```bash
# 使用venv创建虚拟环境
python3.10 -m venv venv

# 激活虚拟环境
# Linux/macOS:
source venv/bin/activate
# Windows:
venv\Scripts\activate
```

### 3. 安装基础依赖
```bash
# 升级pip到最新版本
pip install --upgrade pip

# 安装项目依赖
pip install -r requirements.txt
```

#### 依赖说明：
| 依赖包 | 版本要求 | 用途 |
|--------|---------|------|
| pandas | >=2.0.0 | 数据处理与分析 |
| numpy | >=1.24.0 | 数值计算 |
| pyyaml | >=6.0 | 配置文件解析 |
| requests | >=2.28.0 | HTTP请求（Ollama推理用） |
| tqdm | >=4.64.0 | 进度条显示 |
| scikit-learn | >=1.2.0 | 评测指标计算 |
| llmtuner | >=0.7.1 | 模型微调框架（LLaMA-Factory） |
| transformers | >=4.35.0 | HuggingFace模型加载 |
| torch | >=2.0.0 | PyTorch深度学习框架 |
| accelerate | >=0.24.0 | 分布式训练加速 |
| peft | >=0.6.0 | 低秩适配微调 |
| bitsandbytes | >=0.41.0 | 量化训练支持 |
| matplotlib | >=3.7.0 | 可视化绘图 |
| seaborn | >=0.12.0 | 统计可视化 |
| plotly | >=5.15.0 | 交互式可视化 |

### 4. 验证环境安装
```bash
# 验证Python版本
python --version
# 输出应为 Python 3.10.x 或 3.11.x

# 验证依赖安装
pip list | grep -E "pandas|numpy|torch|transformers|llmtuner"
# 所有依赖包应正常显示对应版本
```

### 5. （可选）GPU环境配置
如果使用GPU进行训练或推理，需要安装对应CUDA版本的PyTorch：
```bash
# CUDA 11.7 版本
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu117

# CUDA 11.8 版本
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1 版本
pip3 install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# 验证CUDA是否可用
python -c "import torch; print(torch.cuda.is_available())"
# 输出为 True 表示CUDA环境配置成功
```

---

## 三、常见环境问题排查
### 1. 依赖安装失败
- **问题**：安装llmtuner或torch时失败
- **解决方案**：
  1. 检查网络连接，可使用国内PyPI镜像：`pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt`
  2. 确认Python版本是否符合要求（必须3.10~3.11，3.12及以上版本可能存在兼容性问题）
  3. 若llmtuner安装失败，可手动从GitHub安装：`pip install git+https://github.com/hiyouga/LLaMA-Factory.git@v0.7.1`

### 2. CUDA不可用
- **问题**：torch.cuda.is_available()返回False
- **解决方案**：
  1. 确认GPU驱动版本与CUDA版本匹配
  2. 确认安装的PyTorch版本对应CUDA版本
  3. 运行`nvidia-smi`检查GPU是否正常识别

### 3. 权限问题
- **问题**：安装依赖时提示权限不足
- **解决方案**：
  1. 使用虚拟环境安装，避免全局安装
  2. 若必须全局安装，添加`--user`参数：`pip install --user -r requirements.txt`
